namespace cOOnsole
{
    /// <summary>
    /// Represents handle status.
    /// Any input is either handled successfully, handled with an error or not handled at all.
    /// </summary>
    public enum HandleStatus
    {
        NotHandled,
        Handled,
        Error,
    }
}